// pure virtual function(순수 가상 함수) : 함수 헤더만 존재하고 함수의 몸체는 없는 함수
// abstract class(추상 클래스) : 순수 가상 함수를 하나라도 가지고 있는 클래스 -> 객체를 생성할 수 없는 클래스
// abstract class는 객체들 사이에 상호 작용하기 위한 인터페이스를 정의하는 용도로 사용할 수 있다.

예제는 Kvector