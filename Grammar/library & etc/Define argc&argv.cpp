/*
명령형 인자(Command line arguments)는 프로그램이 실행될 때 명령어 라인에서 입력된 인자값들을 의미합니다.
이러한 인자는 >>프로그램의 동작을 결정<<하는 데 사용됩니다.

C/C++에서 >> 명령형 인자로 전달된 문자열에서 인덱스를 표현하는 방법 <<은 >> argv 배열과 문자열 인덱싱 연산자 []를 사용<< 하는 것입니다.

argv 배열은 프로그램의 명령행 인수를 저장하는 문자열 배열입니다.
argv[0]은 프로그램의 이름을 나타내고, argv[1]은 첫 번째 명령행 인수를 나타내며, 이후의 인덱스는 두 번째, 세 번째, ... 순서로 지정됩니다.

문자열 인덱싱 연산자 []를 사용하여 argv[1]과 같은 문자열에서 각 문자에 접근할 수 있습니다.
예를 들어, argv[1][0]은 첫 번째 명령행 인수의 첫 번째 문자를 나타내고, argv[1][1]은 첫 번째 명령행 인수의 두 번째 문자를 나타냅니다.

따라서, 문자열에서 인덱스를 표현하는 방법은 다음과 같습니다.

>> argv[1][i] <<

예를 들어, "python my_program.py arg1 arg2"와 같이 명령행에서 실행되는 경우 "arg1"과 "arg2"는 명령형 인자입니다.
이러한 인자는 >>프로그램에서 특정 값을 전달하거나 프로그램의 실행을 제어<<하는 데 사용될 수 있습니다.

argv와 argc는 C++에서 >>main 함수의 인자<< 로 사용되는 변수입니다.

argc는 Argument Count의 약자로, >>명령행 인자(argument)의 개수<<를 나타냅니다.
argc는 정수형 변수이며, 프로그램을 실행할 때 >>명령행 인자를 전달하지 않아도 기본적으로 1의 값<<을 갖습니다.

argv는 Argument Vector의 약자로, >>명령행 인자를 나타내는 문자열 포인터의 배열<<입니다.
argv[0]에는 실행 파일의 이름이 저장되며, >>argv[1]부터 실제 전달된 인자가 저장<<됩니다.
인자의 개수는 argc - 1입니다.

예를 들어, 다음과 같이 실행 파일을 호출할 때 인자를 전달하면 argc와 argv가 어떻게 설정되는지 알 수 있습니다.
*/
#include <iostream>

int main(int argc, char* argv[]) {
  std::cout << "argc = " << argc << "\n";
  for (int i = 0; i < argc; i++) {
    std::cout << "argv[" << i << "] = " << argv[i] << "\n";
  }
  return 0;
}

/* 
위 코드에서 main 함수의 인자로 int argc와 char* argv[]가 사용됩니다.
프로그램을 실행할 때 "test.exe"라는 실행 파일과 >> "arg1"과 "arg2"라는 두 개의 인자를 전달하면, argc는 3<<이 되고
argv[0]에는 "test.exe", argv[1]에는 "arg1", argv[2]에는 "arg2"가 저장됩니다.
따라서 위 코드는 다음과 같은 출력을 생성합니다.
*/

argc = 3
argv[0] = test.exe
argv[1] = arg1
argv[2] = arg2

/*
이렇게 명령행 인자를 활용하면, 프로그램 실행 시 동적으로 값을 변경할 수 있습니다.
예를 들어, 파일 이름, 입출력 경로 등의 정보를 전달할 수 있습니다.
*/