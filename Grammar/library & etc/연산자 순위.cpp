Q. cout << k % 10 << endl;과 cout << (k % 10) << endl;는 무슨차이야?
//
A. 두 개의 출력문은 같은 결과를 출력합니다.

하지만, 연산자 우선순위 때문에 괄호를 쓰지 않으면, % 연산자가 << 연산자보다 먼저 실행됩니다.

따라서, cout << k % 10 << endl;에서는 k % 10 연산이 먼저 실행되고, 그 결과에 endl을 추가합니다.

하지만, cout << (k % 10) << endl;에서는 k % 10 연산 결과를 괄호로 묶어서, 먼저 출력하고 그 다음에 endl을 추가합니다.

즉, 결과적으로는 같은 결과를 출력하지만, 코드를 더 명확하고 가독성 높게 작성하기 위해서는, 괄호를 사용하는 것이 좋습니다.